{
  "kind": "work",
  "id": "task-124",
  "title": "Localize errors-found summary and fix its pluralization (1st of 2 occurrences, reuse)",
  "file": "app/src/main/java/com/example/ui/screens/SettingsDialog.kt",
  "structure_type": "block",
  "change_type": "modify",
  "description": "Fixes a latent bug where the manual '> 1' check mishandled proper plural rules. This exact literal also appears in DataBackupImportDialog further down the file; occurrence=1 resolves to whichever copy is still literal when this task runs — this one, since it's earlier in the file.",
  "acceptance_criteria": [
    "The hardcoded string literal is replaced by the referenced stringResource/pluralStringResource call",
    "No other line is modified"
  ],
  "start_anchor": "\"${result.errors.size} error${if (result.errors.size > 1) \"s\" else \"\"} found\",",
  "exact_code": "pluralStringResource(R.plurals.errors_found_count, result.errors.size, result.errors.size),",
  "occurrence": 1
}
