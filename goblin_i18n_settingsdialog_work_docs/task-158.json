{
  "kind": "work",
  "id": "task-158",
  "title": "Localize the restore confirm button and fix its always-plural bug",
  "file": "app/src/main/java/com/example/ui/screens/SettingsDialog.kt",
  "structure_type": "block",
  "change_type": "modify",
  "description": "Fixes a latent bug where this button always said 'Tasks', even for exactly 1.",
  "acceptance_criteria": [
    "The hardcoded string literal is replaced by the referenced stringResource/pluralStringResource call",
    "No other line is modified"
  ],
  "start_anchor": "Text(\"Restore ${result?.payload?.tasks?.size ?: 0} Tasks\", fontWeight = FontWeight.Bold)",
  "exact_code": "Text(pluralStringResource(R.plurals.restore_tasks_button_count, result?.payload?.tasks?.size ?: 0, result?.payload?.tasks?.size ?: 0), fontWeight = FontWeight.Bold)"
}
