{
  "kind": "work",
  "id": "task-156",
  "title": "Localize tasks-ready-to-restore summary and fix its pluralization",
  "file": "app/src/main/java/com/example/ui/screens/SettingsDialog.kt",
  "structure_type": "block",
  "change_type": "modify",
  "description": "Replace the hardcoded literal with a resource-backed lookup.",
  "acceptance_criteria": [
    "The hardcoded string literal is replaced by the referenced stringResource/pluralStringResource call",
    "No other line is modified"
  ],
  "start_anchor": "\"${result.payload.tasks.size} task${if (result.payload.tasks.size != 1) \"s\" else \"\"} ready to restore\",",
  "exact_code": "pluralStringResource(R.plurals.tasks_ready_restore_count, result.payload.tasks.size, result.payload.tasks.size),"
}
