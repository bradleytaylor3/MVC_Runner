{
  "kind": "work",
  "id": "task-132",
  "title": "Localize the chain-order badge",
  "file": "app/src/main/java/com/example/ui/screens/SettingsDialog.kt",
  "structure_type": "block",
  "change_type": "modify",
  "description": "This forEach body runs inside TaskImportDialog's @Composable scope, so stringResource() can be called directly here.",
  "acceptance_criteria": [
    "The hardcoded string literal is replaced by the referenced stringResource/pluralStringResource call",
    "No other line is modified"
  ],
  "start_anchor": "if (task.chainId != null) badges.add(\"chain #${task.chainOrder}\")",
  "exact_code": "if (task.chainId != null) badges.add(stringResource(R.string.chain_number_badge, task.chainOrder ?: 0))"
}
