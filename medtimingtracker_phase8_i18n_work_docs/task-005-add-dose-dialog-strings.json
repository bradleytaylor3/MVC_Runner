{
  "kind": "work",
  "id": "task-005",
  "title": "Use string resources in AddDoseDialog composable",
  "file": "app/src/main/java/com/taylordproductivty/med_timing_tracker/ui/components/AddDoseDialog.kt",
  "structure_type": "function",
  "change_type": "modify",
  "name": "AddDoseDialog",
  "description": "Replace hardcoded literals with stringResource() calls. Function signature, state, and logic are unchanged -- only the literal Text/label arguments.",
  "acceptance_criteria": [
    "Dialog title uses stringResource(R.string.log_a_dose)",
    "'Who' and 'Medication' section labels use who_label / medication_label",
    "Dose amount field label uses dose_given_label",
    "Confirm button uses stringResource(R.string.log_dose)",
    "Cancel button uses the shared cancel resource",
    "onSubmit callback signature and all state/logic are byte-for-byte unchanged from before"
  ],
  "exact_code": "@Composable\nfun AddDoseDialog(\n    profiles: List<Profile>,\n    medications: List<Medication>,\n    initialProfileId: Int?,\n    onDismiss: () -> Unit,\n    onSubmit: (profileId: Int, medicationId: Int, doseAmount: String) -> Unit,\n) {\n    var selectedProfile by remember {\n        mutableStateOf(profiles.firstOrNull { it.id == initialProfileId } ?: profiles.firstOrNull())\n    }\n    var selectedMedication by remember { mutableStateOf(medications.firstOrNull()) }\n    var doseAmount by remember(selectedMedication) {\n        mutableStateOf(selectedMedication?.defaultDose.orEmpty())\n    }\n\n    AlertDialog(\n        onDismissRequest = onDismiss,\n        title = { Text(stringResource(R.string.log_a_dose)) },\n        text = {\n            Column(modifier = Modifier.fillMaxWidth()) {\n                Text(\n                    stringResource(R.string.who_label),\n                    style = MaterialTheme.typography.labelLarge,\n                )\n                Row(\n                    horizontalArrangement = Arrangement.spacedBy(8.dp),\n                    modifier = Modifier\n                        .fillMaxWidth()\n                        .horizontalScroll(rememberScrollState())\n                        .padding(top = 4.dp),\n                ) {\n                    profiles.forEach { profile ->\n                        ProfileChip(\n                            profile = profile,\n                            selected = profile.id == selectedProfile?.id,\n                            onClick = { selectedProfile = profile },\n                        )\n                    }\n                }\n\n                Text(\n                    stringResource(R.string.medication_label),\n                    style = MaterialTheme.typography.labelLarge,\n                    modifier = Modifier.padding(top = 16.dp),\n                )\n                Row(\n                    horizontalArrangement = Arrangement.spacedBy(8.dp),\n                    modifier = Modifier\n                        .fillMaxWidth()\n                        .horizontalScroll(rememberScrollState())\n                        .padding(top = 4.dp),\n                ) {\n                    medications.forEach { medication ->\n                        MedicationChip(\n                            medication = medication,\n                            selected = medication.id == selectedMedication?.id,\n                            onClick = { selectedMedication = medication },\n                        )\n                    }\n                }\n\n                OutlinedTextField(\n                    value = doseAmount,\n                    onValueChange = { doseAmount = it },\n                    label = { Text(stringResource(R.string.dose_given_label)) },\n                    modifier = Modifier\n                        .fillMaxWidth()\n                        .padding(top = 16.dp),\n                )\n            }\n        },\n        confirmButton = {\n            Button(\n                onClick = {\n                    val profile = selectedProfile\n                    val medication = selectedMedication\n                    if (profile != null && medication != null) {\n                        onSubmit(profile.id, medication.id, doseAmount)\n                    }\n                },\n            ) {\n                Text(stringResource(R.string.log_dose))\n            }\n        },\n        dismissButton = {\n            TextButton(onClick = onDismiss) {\n                Text(stringResource(R.string.cancel))\n            }\n        },\n    )\n}"
}
