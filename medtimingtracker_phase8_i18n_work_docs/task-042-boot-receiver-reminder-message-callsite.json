{
  "kind": "work",
  "id": "task-042",
  "title": "Pass Context into ReminderMessage.build() call in BootReminderReceiver",
  "file": "app/src/main/java/com/taylordproductivty/med_timing_tracker/notifications/BootReminderReceiver.kt",
  "structure_type": "block",
  "change_type": "modify",
  "start_anchor": "                    val (title, text) = ReminderMessage.build(medication.name, nextEligible)",
  "description": "ReminderMessage.build's signature now requires a Context (task-041) -- appContext is already in scope in this coroutine.",
  "acceptance_criteria": [
    "The call is ReminderMessage.build(appContext, medication.name, nextEligible)",
    "Nothing else on this line or around it changes"
  ],
  "exact_code": "                    val (title, text) = ReminderMessage.build(appContext, medication.name, nextEligible)"
}
