{
  "kind": "work",
  "id": "task-046",
  "title": "Localize ActiveTaskItem's stacked-habit count text and fix its pluralization",
  "file": "app/src/main/java/com/example/ui/components/TaskItem.kt",
  "structure_type": "block",
  "change_type": "modify",
  "description": "Replace the manually-pluralized template (which only ever appended a bare 's', mishandling non-English plural rules) with a real <plurals> resource.",
  "acceptance_criteria": [
    "The hardcoded string template is replaced by pluralStringResource(R.plurals.stacked_habits_count, chainCount, chainCount)",
    "No other line is modified"
  ],
  "start_anchor": "\"${chainCount} stacked habit${if (chainCount > 1) \"s\" else \"\"}\",",
  "exact_code": "pluralStringResource(R.plurals.stacked_habits_count, chainCount, chainCount),"
}
