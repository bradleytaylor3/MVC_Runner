{
  "kind": "work",
  "id": "task-059",
  "title": "Localize UpcomingQuestItem's 'Edit schedule' content description (1st of 2 occurrences)",
  "file": "app/src/main/java/com/example/ui/components/TaskItem.kt",
  "structure_type": "block",
  "change_type": "modify",
  "description": "Replace the hardcoded literal with a resource-backed lookup. This exact literal also appears in RepeatQuestItem further down the file; this task's occurrence=1 resolves against whichever copy is still literal at the time this task runs, which — given this batch's tasks apply in file order — is UpcomingQuestItem's (the earlier one in the file).",
  "acceptance_criteria": [
    "The hardcoded string literal is replaced by stringResource(R.string.edit_schedule_content_description)",
    "No other line is modified"
  ],
  "start_anchor": "Icon(Icons.Filled.Edit, contentDescription = \"Edit schedule\", tint = MaterialTheme.colorScheme.primary, modifier = Modifier.size(20.dp))",
  "occurrence": 1,
  "exact_code": "Icon(Icons.Filled.Edit, contentDescription = stringResource(R.string.edit_schedule_content_description), tint = MaterialTheme.colorScheme.primary, modifier = Modifier.size(20.dp))"
}
