{
  "kind": "work",
  "id": "task-049",
  "title": "Localize ActiveTaskItem's 'Remove step' content description",
  "file": "app/src/main/java/com/example/ui/components/TaskItem.kt",
  "structure_type": "block",
  "change_type": "modify",
  "description": "Replace the hardcoded literal with a resource-backed lookup.",
  "acceptance_criteria": [
    "The hardcoded string literal is replaced by stringResource(R.string.remove_step_content_description)",
    "No other line is modified"
  ],
  "start_anchor": "Icon(Icons.Filled.Close, contentDescription = \"Remove step\", tint = MaterialTheme.colorScheme.error.copy(alpha = 0.6f), modifier = Modifier.size(14.dp))",
  "exact_code": "Icon(Icons.Filled.Close, contentDescription = stringResource(R.string.remove_step_content_description), tint = MaterialTheme.colorScheme.error.copy(alpha = 0.6f), modifier = Modifier.size(14.dp))"
}
