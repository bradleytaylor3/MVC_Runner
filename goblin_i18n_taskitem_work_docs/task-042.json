{
  "kind": "work",
  "id": "task-042",
  "title": "Localize ActiveTaskItem's 'Notes' label",
  "file": "app/src/main/java/com/example/ui/components/TaskItem.kt",
  "structure_type": "block",
  "change_type": "modify",
  "description": "Replace the hardcoded literal with a resource-backed lookup.",
  "acceptance_criteria": [
    "The hardcoded string literal is replaced by stringResource(R.string.notes_label)",
    "No other line is modified"
  ],
  "start_anchor": "Text(\"Notes\", fontSize = 11.sp, fontWeight = FontWeight.SemiBold, color = MaterialTheme.colorScheme.onSurface.copy(alpha = 0.5f))",
  "exact_code": "Text(stringResource(R.string.notes_label), fontSize = 11.sp, fontWeight = FontWeight.SemiBold, color = MaterialTheme.colorScheme.onSurface.copy(alpha = 0.5f))"
}
