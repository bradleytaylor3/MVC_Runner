{
  "kind": "work",
  "id": "task-002",
  "title": "Change InfoSection to hold string-resource IDs instead of resolved strings",
  "file": "app/src/main/java/com/example/ui/InfoDialog.kt",
  "structure_type": "block",
  "change_type": "modify",
  "description": "InfoSection's fields hold resource IDs now (plain Int constants, legal in this top-level, non-composable list), not resolved text. This must run before ResolvedInfoSection is added (next task) — that new class also has title/body String fields, and adding it first would make this task's anchor ambiguous.",
  "acceptance_criteria": [
    "InfoSection's fields are renamed/retyped to titleRes: Int and bodyRes: Int",
    "No other part of the data class changes"
  ],
  "start_anchor": "val title: String,",
  "end_anchor": "val body: String",
  "exact_code": "val titleRes: Int,\n    val bodyRes: Int"
}
