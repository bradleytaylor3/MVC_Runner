{
  "kind": "work",
  "id": "task-023",
  "title": "Localize 'Alert at' time-picker label (reuse)",
  "file": "app/src/main/java/com/example/ui/screens/QuestsManagementScreen.kt",
  "structure_type": "block",
  "change_type": "modify",
  "description": "Replace the hardcoded label literal passed into TimePickerRow with the shared label_alert_at resource (already introduced by ActiveQuestsScreen.kt's batch).",
  "acceptance_criteria": [
    "The hardcoded string literal is replaced by stringResource(R.string.label_alert_at)",
    "No other argument on this line is modified"
  ],
  "start_anchor": "TimePickerRow(label = \"Alert at\", hour = reminderHour, minute = reminderMinute, onTimeChanged = { h, m -> reminderHour = h; reminderMinute = m })",
  "exact_code": "TimePickerRow(label = stringResource(R.string.label_alert_at), hour = reminderHour, minute = reminderMinute, onTimeChanged = { h, m -> reminderHour = h; reminderMinute = m })"
}
