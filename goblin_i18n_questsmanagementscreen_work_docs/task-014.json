{
  "kind": "work",
  "id": "task-014",
  "title": "Localize weekday abbreviation labels",
  "file": "app/src/main/java/com/example/ui/screens/QuestsManagementScreen.kt",
  "structure_type": "block",
  "change_type": "modify",
  "description": "Replace the hardcoded weekday-letter list with the shared string-array resource so the day-selector labels localize.",
  "acceptance_criteria": [
    "val dayLabels is assigned from stringArrayResource(R.array.weekday_abbreviations) instead of a hardcoded listOf(...)",
    "dayLabels remains indexable the same way (dayLabels[index]) for the existing day-selector Row"
  ],
  "start_anchor": "val dayLabels = listOf(\"S\", \"M\", \"T\", \"W\", \"T\", \"F\", \"S\")",
  "exact_code": "val dayLabels = stringArrayResource(R.array.weekday_abbreviations)"
}
