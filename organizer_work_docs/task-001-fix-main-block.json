{
  "kind": "work",
  "id": "task-001",
  "title": "Fix corrupted __main__ block",
  "file": "organizer.py",
  "structure_type": "block",
  "change_type": "modify",
  "start_anchor": "if __name__ == \"__main__\":",
  "end_anchor": "exit $__ollama_status",
  "exact_code": "if __name__ == \"__main__\":\n    process_images()",
  "description": "The file currently ends with a truncated call `process_` followed by stray shell script lines (__ollama_status=$?, a pwd -P redirect, exit $__ollama_status) that are not valid Python — replace the whole block with a correct __main__ guard.",
  "acceptance_criteria": [
    "File ends with a valid if __name__ == '__main__': block that calls process_images()",
    "No shell script lines (__ollama_status, pwd -P, exit) remain anywhere in the file",
    "The file parses as valid Python"
  ]
}
