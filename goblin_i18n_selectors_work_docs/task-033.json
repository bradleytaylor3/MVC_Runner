{
  "kind": "work",
  "id": "task-033",
  "title": "Localize GoblinDatePickerDialog's Cancel button (1st of 2 occurrences, reuse)",
  "file": "app/src/main/java/com/example/ui/components/Selectors.kt",
  "structure_type": "block",
  "change_type": "modify",
  "description": "Replace the hardcoded literal with the shared action_cancel resource. GoblinTimePickerDialog further down this file has this exact same line; this task's occurrence=1 resolves against whichever copy is still literal at the time this task runs, which — given this batch's tasks apply in file order — is this one (the earlier one in the file).",
  "acceptance_criteria": [
    "The hardcoded string literal is replaced by stringResource(R.string.action_cancel)",
    "No other line is modified"
  ],
  "start_anchor": "TextButton(onClick = onDismiss) { Text(\"Cancel\") }",
  "occurrence": 1,
  "exact_code": "TextButton(onClick = onDismiss) { Text(stringResource(R.string.action_cancel)) }"
}
