{
  "kind": "work",
  "id": "task-034",
  "title": "Localize TimePickerRow's AM/PM markers",
  "file": "app/src/main/java/com/example/ui/components/Selectors.kt",
  "structure_type": "function",
  "change_type": "modify",
  "description": "Replace the hardcoded AM/PM literals with resource-backed lookups.",
  "acceptance_criteria": [
    "AM comes from stringResource(R.string.am_label)",
    "PM comes from stringResource(R.string.pm_label)",
    "No other logic in the function is changed"
  ],
  "name": "TimePickerRow",
  "exact_code": "@Composable\n@OptIn(ExperimentalMaterial3Api::class)\nfun TimePickerRow(\n    label: String,\n    hour: Int,\n    minute: Int,\n    onTimeChanged: (Int, Int) -> Unit\n) {\n    var showDialog by remember { mutableStateOf(false) }\n    val displayTime = String.format(\"%d:%02d %s\",\n        if (hour % 12 == 0) 12 else hour % 12,\n        minute,\n        if (hour < 12) stringResource(R.string.am_label) else stringResource(R.string.pm_label)\n    )\n\n    Row(\n        modifier = Modifier.fillMaxWidth(),\n        horizontalArrangement = Arrangement.SpaceBetween,\n        verticalAlignment = Alignment.CenterVertically\n    ) {\n        Text(\n            text = label,\n            fontWeight = FontWeight.Medium,\n            fontSize = 14.sp,\n            color = MaterialTheme.colorScheme.onSurface\n        )\n\n        OutlinedButton(\n            onClick = { showDialog = true },\n            shape = RoundedCornerShape(8.dp),\n            contentPadding = PaddingValues(horizontal = 16.dp, vertical = 6.dp)\n        ) {\n            Text(displayTime, fontWeight = FontWeight.Bold)\n        }\n    }\n\n    if (showDialog) {\n        GoblinTimePickerDialog(\n            initialHour = hour,\n            initialMinute = minute,\n            onDismiss = { showDialog = false },\n            onConfirm = { h, m ->\n                onTimeChanged(h, m)\n                showDialog = false\n            }\n        )\n    }\n}"
}
