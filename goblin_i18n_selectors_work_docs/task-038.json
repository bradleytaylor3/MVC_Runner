{
  "kind": "work",
  "id": "task-038",
  "title": "Localize GoblinTimePickerDialog's OK button (reuse)",
  "file": "app/src/main/java/com/example/ui/components/Selectors.kt",
  "structure_type": "block",
  "change_type": "modify",
  "description": "Replace the hardcoded literal with the shared action_ok resource.",
  "acceptance_criteria": [
    "The hardcoded string literal is replaced by stringResource(R.string.action_ok)",
    "No other line is modified"
  ],
  "start_anchor": "TextButton(onClick = { onConfirm(state.hour, state.minute) }) { Text(\"OK\") }",
  "exact_code": "TextButton(onClick = { onConfirm(state.hour, state.minute) }) { Text(stringResource(R.string.action_ok)) }"
}
